# Project Status & Design Overview

**Current Branch:** `feature/rest-apis`

## 📁 Project Structure (Refactored)
We have moved to a Monorepo structure:
- **`platform-frontend/`**: The React/Vanilla JS frontend.
- **`media-server/`**: Node.js server for Mediasoup (SFU) & static file serving.
- **`platform-backend/`**: Spring Boot backend (Port 8080).

## 🗄️ Database Schema Plan (PostgreSQL)
We are using **Inheritance** for Users.

1.  **`users`**: Base table (email, password, role).
2.  **`candidates`**: Extends `users` (skills, resume_url).
3.  **`interviewers`**: Extends `users` (company_name).
4.  **`interviews`** (New): Tracks scheduled meetings between candidates and interviewers.

## 🔌 REST API Plan

### 1. User Management
- `GET /api/users/profile`: Get current user details.
- `PUT /api/users/profile`: Update clear profile info.

### 2. Candidate APIs
- `GET /api/candidates/dashboard`: Dashboard stats.
- `PUT /api/candidates/skills`: Update skills.

### 3. Interview APIs (Coming Soon)
- `POST /api/interviews/schedule`: Schedule new interviews.
- `GET /api/interviews/my-interviews`: List upcoming sessions.

## 🛠️ Work In Progress
- **Refactoring `profile.js`**: Currently uses `localStorage`. Will be updated to use the real API.
- **Interviewer UI**: The dashboard and scheduling pages are missing and need to be created.

---
*Generated by Antigravity Agent - 2026-01-21*
